# Build wheels and sdist and publish new version of the package to PyPI.

name: Publish package (PyPI)

on:
  release:
    branches: [main]
    types: [published]
  workflow_dispatch:

jobs:
  build-wheels:
    runs-on: ${{ matrix.os }} 
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-20.04, windows-2019, macOS-10.15]

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.1.1
        with:
          output-dir: dist
        env:
          CIBW_BUILD: cp36-* cp37-* cp38-* cp39-* cp310-* pp37-*
          CIBW_SKIP: cp310-macosx_*  pp37-macosx_* # skipping these for now, they result in a build error (https://github.com/pypa/cibuildwheel/issues/402)
          CIBW_ARCHS: auto
          CIBW_BUILD_FRONTEND: pip
          CIBW_TEST_REQUIRES: pytest
          CIBW_TEST_COMMAND: pytest {project}/tests

      - name: Upload wheels to artifact
        uses: actions/upload-artifact@v2
        with:
          name: dist
          path: ./dist/*.whl

  build-sdist:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
      
      - name: Build source distribution
        run: 
          python -m build --sdist

      - name: Upload source distribution to artifact
        uses: actions/upload-artifact@v2
        with:
          name: dist
          path: ./dist/*.tar.gz

  publish-wheels:
    runs-on: ubuntu-latest
    needs: [build-wheels, build-sdist]

    steps:
      - name: Download wheels from artifact
        uses: actions/download-artifact@v2
        with:
          name: dist
          path: ./dist

      - name: Publish distribution to PyPI
        uses: pypa/gh-action-pypi-publish@master
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}
          packages_dir: dist
